{
  "name": "pizza",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "tailwindcss -i ./src/input.css -o ./public/style.css --watch",
    "build": "npx tailwindcss -i ./src/input.css -o ./public/style.css --minify && xcopy /E /I /Y public dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "tailwindcss": "^3.4.17"
  }
}
